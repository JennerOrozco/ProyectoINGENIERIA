import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IngenieriaSoftwareUMG',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
